{
  "name": "@appup/network",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "develop": "nodemon -e js,ts --watch src --exec \"yarn run build && yarn run start\""
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^13.11.0",
    "nodemon": "^2.0.3",
    "typescript": "^3.8.3"
  }
}
